<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <!-- Set the title of the document -->
    <title>
        <%= title %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- Set the icon for the web page -->
    <link rel="icon" type="image/png" href="/image/logo.png" />
    <!-- Link the CSS layout file to the document -->
    <link rel="stylesheet" href="/css/layout.css" />
    <!-- Link the Font Awesome CSS file to the document -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" />
    <!-- Link the Noty CSS file to the document -->
    <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css" />
    <!-- Load the Noty JS library -->
    <script rel="javascript" src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script>
</head>

<body>
    <!-- Include content from the respective views -->
    <%- body %>
        <!-- Display notifications using the Noty library -->
        <script>
      <% if(flash.success && flash.success.length > 0) { %>
                new Noty({
                    then: 'relax',
                    text: "<%= flash.success %>",
                    type: 'success',
                    layout: 'topRight',
                    timeout: 1500
                }).show()
                <% } %>
           <% if (flash.error && flash.error.length > 0) {%>
                new Noty({
                    then: 'relax',
                    text: "<%= flash.error %>",
                    type: 'error',
                    layout: 'topRight',
                    timeout: 1500
                }).show()
                <% } %>
        </script>
</body>

</html>